import css from './Buttons.module.css';

const StartLosingBtn = () => {
    return (
        <button type='submit' className={css.startLosingBtn}>
            Start losing weight
        </button>
    )
};
export default StartLosingBtn;